import React from 'react'

const Searchbar = () => {
    return (
        <div className="search">
            <input type="text" name="" id="searchbar" placeholder="Enter address, zip, city" />
            <button>Search</button>
        </div>
    )
}

export default Searchbar
